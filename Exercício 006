#include <stdio.h>

int main(){
    int x, y, z;
    printf("Digite as horas, os minutos e os segundos:\n");
    scanf("%d %d %d", &x, &y, &z);

    printf("Total de segundos: %d\n", x*3600 + y*60 + z);

    return 0;
}
