#include <stdio.h>

int main(){
    float x;
    printf("Digite um número qualquer:\n");
    scanf("%f", &x);

    x = x * ((x >= 0) - (x < 0));
    printf("Módulo do número: %.2f\n", x);

    return 0;
}
